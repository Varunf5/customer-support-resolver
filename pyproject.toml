[project]
name = "customer-support-resolver"
version = "0.1.0"
description = "Customer Support AI OpenEnv Environment"
authors = [{ name = "Varun" }]
dependencies = [
    "flask",
    "openai"
]

[tool.openenv]
env = "env.environment:SupportTicketEnvironment"
entrypoint = "inference.py"