[project]
name = "customer-support-resolver"
version = "0.1.0"
dependencies = [
    "flask",
    "openenv>=0.2.0"
]

[project.scripts]
server = "server.app:main"

[tool.openenv]
env = "env.environment:SupportTicketEnvironment"